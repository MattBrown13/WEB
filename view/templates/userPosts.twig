<h3>Seznam mých příspěvků</h3>
<div id="tabulka">
    <table style="width:100%" class="table-bordered">
        <thead>
            <tr>
                <th style="width:60%">Název vlákna</th>
                <th style="width:40%">Autor vlákna</th>		
            </tr>
        </thead>
        <tbody>
            {% for post in userPosts %}
                <tr>
                    <td><a href="index.php?page=userPost&postName={{post.post_name}}">{{post.post_name}}</a></td>
                    <td>{{post.nick}}</td>		
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>

